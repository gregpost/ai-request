<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Blockchain Prices</title>
  <style>
    body { font-family: Arial, sans-serif; padding: 20px; }
    table { border-collapse: collapse; width: 50%; }
    th, td { border: 1px solid #ccc; padding: 8px; text-align: left; }
    th { background-color: #f4f4f4; }
  </style>
</head>
<body>
  <h1>Blockchain Prices</h1>
  <table id="priceTable">
    <thead>
      <tr>
        <th>Blockchain</th>
        <th>Price (USD)</th>
      </tr>
    </thead>
    <tbody></tbody>
  </table>

  <script>
    const blockchains = ["bitcoin", "ethereum", "cardano", "solana"];

    async function fetchPrices() {
      try {
        const ids = blockchains.join(",");
        const response = await fetch(`https://api.coingecko.com/api/v3/simple/price?ids=${ids}&vs_currencies=usd`);
        const data = await response.json();
        updateTable(data);
      } catch (err) {
        console.error("Error fetching prices:", err);
      }
    }

    function updateTable(data) {
      const tbody = document.querySelector("#priceTable tbody");
      tbody.innerHTML = "";
      blockchains.forEach(chain => {
        const row = document.createElement("tr");
        const nameCell = document.createElement("td");
        nameCell.textContent = chain.charAt(0).toUpperCase() + chain.slice(1);
        const priceCell = document.createElement("td");
        priceCell.textContent = data[chain]?.usd ? `$${data[chain].usd}` : "N/A";
        row.
